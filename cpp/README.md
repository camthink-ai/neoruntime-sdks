# NE503 AIPC C++ SDK

This directory is reserved for the future C++ SDK.

The public documentation already includes C++ SDK placeholder pages so links can
remain stable while the C++ SDK design is finalized.
