target-version = "py38"
line-length = 100
extend-exclude = ["hailo_ipc_sdk/proto"]

[lint]
select = ["E", "F", "I", "UP", "B"]
ignore = ["E501"]  # line length handled by formatter

[lint.per-file-ignores]
"tests/*" = ["F401"]
